package com.neon.android.ui.theme

import androidx.compose.ui.graphics.Color

/** Chat accent colors aligned with NLOON palette (purple / orange / green). */
object ChatColors {
    val meshAccent = Color(0xFF8A2BE2)
    val locationAccent = Color(0xFF00C851)
    val channelAccent = Color(0xFFFFA500)
    val privateAccent = Color(0xFF9D4EDD)
    val selfBubble = Color(0xFF8A2BE2)
    val selfBubbleContent = Color.White
    val peerBubble = Color(0xFFF0F0F5)
    val peerBubbleContent = Color(0xFF333333)
    val unreadBadge = Color(0xFFFFA500)
}
